$('#current-user-activties').html("<%= j render partial: 'user_interests', collection: Array(current_user.interests.sort {|x,y| x.user_rating <=> y.user_rating }).reverse, as: :interest %>")
$("#activity-options").html("<%= j render partial: 'add_interest', collection: Activity.all, as: :activity %>")
$('.edit_interest').editInterest()
$('.new_interest').editInterest()
$('.on-click').addOnClickActivities()
$('#flash').html("<%= j raw(flash_display) %>")
$('.flash').flashExitListen();