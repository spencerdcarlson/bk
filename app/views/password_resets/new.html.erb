<h1>Reset Password</h1>
<%= form_tag password_resets_path, method: 'post' do %>
	<section class='field'>
		<%= label_tag :email %>
		<%= text_field_tag :email, params[:email] %>
	</section>
	<section class='actions'><%= submit_tag 'Reset' %></seciton>
<% end %>